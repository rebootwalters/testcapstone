<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - My ASP.NET Application</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
</head>
<body>
    <div class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                @Html.ActionLink("Brian at 3PM", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
            </div>
            <div class="navbar-collapse collapse">
                <ul class="nav navbar-nav">
                    <li>@Html.ActionLink("Home", "Index", "Home")</li>
                    <li>@Html.ActionLink("About", "About", "Home")</li>
                    <li>@Html.ActionLink("Contact", "Contact", "Home")</li>
                    @if (User.IsInRole(MagicConstants.AdminRoleName))
                    {
                        <li>@Html.ActionLink("Roles", "Index", "Role")</li>
                        <li>@Html.ActionLink("Users", "Index", "User")</li>
                        <li>@Html.ActionLink("Items", "Index", "Item")</li>
                        <li>@Html.ActionLink("Michael", "Index", "Michael")</li>

                    }
                    @if (User.Identity.IsAuthenticated)
                    {
                        <li>@Html.ActionLink("My Items", "Index", "MyItem")</li>
                    }
                    <li>@Html.ActionLink("Testing", "Index", "ErrorTest")</li>
                    @if (User.Identity.IsAuthenticated)
                    {
                        <li>@Html.ActionLink("Welcome " + User.Identity.Name + ":" + User.Identity.AuthenticationType, "Index", "Home") </li>
                        <li>@Html.ActionLink("Logout", "Logout", "Home")</li>
                        if (User.Identity.AuthenticationType.StartsWith("ClearText"))
                        {
                            <li>@Html.ActionLink("HASH Password", "Hash", "Home")</li>

                        }
                    }
                    else
                    {
                        <li>@Html.ActionLink("Register", "Register", "Home")</li>

                        <li>@Html.ActionLink("Login", "Login", "Home")</li>
                    }
                </ul>
            </div>
        </div>
    </div>
    <div class="container body-content">
        Brian was Here
        @RenderBody()
        <hr />
        <footer>
            <p>&copy; @DateTime.Now.Year - Brian's ASP.NET Application</p>
        </footer>
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
