@model IEnumerable<BusinessLogicLayer.UserBLL>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>@Html.Partial("Paging") </p>

<p>
    @Html.ActionLink("Create New", "Create")
</p>


    <table class="table">
        <tr>
            <th></th>
            <th></th>
            <th></th>
            <th></th>
            <th>
                @Html.DisplayNameFor(model => model.EMail)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateOfBirth)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RoleID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RoleName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Age)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.isAbleToPurchaseCigarettes)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Hash)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Salt)
            </th>

            <th></th>
        </tr>

        @foreach (var item in Model)
        {
    <tr>
        <td>
            @if (User.IsInRole(MagicConstants.AdminRoleName))
            {
                @Html.ActionLink("Edit", "Edit", new { id = item.UserID })
                
            }
        </td>
        <td>
            @Html.ActionLink("Details", "Details", new { id = item.UserID })
        </td>
        <td>
            @if (User.IsInRole(MagicConstants.AdminRoleName))
            {
              
                @Html.ActionLink("Delete", "Delete", new { id = item.UserID })
            }
        </td>
        <td>
            @if (User.IsInRole(MagicConstants.AdminRoleName))
            {

                @Html.ActionLink("Impersonate", "Impersonate", new { id = item.UserID })
            }
        </td>

        
        <td>
            @Html.DisplayFor(modelItem => item.EMail)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateOfBirth)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RoleID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RoleName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Age)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.isAbleToPurchaseCigarettes)
        </td>


        <td>
            @Html.DisplayFor(modelItem => item.Hash)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Salt)
        </td>


    </tr>
        }

    </table>
